apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: redirect-hpa
  namespace: urlsnip
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: redirect
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50